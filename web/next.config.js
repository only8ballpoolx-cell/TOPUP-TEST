/** @type {import('next').NextConfig} */
const nextConfig = {
  // Deploying to Vercel: leave this as-is, Vercel runs Next.js natively.
  // Deploying to Firebase Hosting instead: uncomment the line below to
  // produce a static export in web/out (matches firebase.json "public").
  // output: "export",
};
module.exports = nextConfig;
